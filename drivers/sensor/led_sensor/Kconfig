# SPDX-License-Identifier: Apache-2.0

config LED_SENSOR
    bool "LED sensor driver"
    default y
    depends on GPIO
    depends on SENSOR
    help
      Simple sensor driver using an onboard LED.
